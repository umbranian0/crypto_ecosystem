# Backlog — economic-service

Source: `docs/da-tese-ao-produto.md` (sections 1.5, 2.3.5, 2.4, 2.6, 2.7 — read directly, not paraphrased), `docs/solution-design.md` (section 1, 5, and the "Subsystem 5 deliberately built last" framing in the header), `docs/implementation-plan.md` (sections 2, 5, 6, 7, 9), `services/economic-service/README.md` (current deferred stub), `services/validation-service/README.md` (Contract/Routes/Data-model sections — read to confirm what currently exists to integrate against), `docs/tickets/README.md` (full ticket index, all modules).

Scope: `services/economic-service` scaffolding only. In scope: service skeleton, `economic.*` schema + Repository pattern for cost/slippage *inputs*, request/response contracts, a mocked-upstream integration path, and — as the actual point of this backlog — a structural, code-enforced eligibility gate that prevents any profitability/return/P&L number from ever being computed or returned outside a real, verified "model beat naive" result. Out of scope (explicit Won'ts below): real `validation-service` integration, any UI, real report/output distribution, JWT/session auth beyond whatever gateway-api-style stub is needed for scaffolding to run.

## Override note (read this before the stories)

**Trigger #11 has NOT fired**, and this is a materially different kind of override than every other trigger override already recorded in this repo (`gateway-api` trigger #5, `dashboard-web` trigger #8, `ingestion-service` connectors trigger #6/#10). Those were all soft build-order triggers whose condition was simply "no pilot client yet" — a scheduling convenience, not a normative constraint. Trigger #11 is different in kind: `docs/implementation-plan.md` section 6 states it exists "per the ethical boundary in docs section 2.7," and `docs/da-tese-ao-produto.md` section 2.7 states plainly: *"Qualquer extensão para sinais de trading exige o módulo económico (custos, slippage) antes de qualquer alegação de rentabilidade"* and *"deixar claro... a distinção precisão estatística vs. lucro económico."* Section 2.3.5 states the module "só ativado depois de um modelo passar o subsistema 1." This is a business-honesty constraint tied directly to the platform's own core finding (section 1.4: no model beat Naive0 in a stable, significant way at any horizon; section 1.5: R² out-of-sample negative everywhere; the thesis explicitly never tested trading profit).

**What I checked, and what I found**, per the requester's explicit instruction to confirm this directly rather than assume:

1. **No model has ever beaten naive in a real run on this platform.** `services/validation-service/README.md`'s "Data model" section states plainly that `split_results.model_*`/`dm_*` currently map to the **`"naive_last"`** baseline — i.e. every run to date compares Naive0 against NaiveLast, not against any client-supplied predictive model. `VS-011`'s own regression test is described as reproducing "a construction-guaranteed NaiveLast-vs-Naive0 DM-verdict pattern" against the thesis's own published fixture constants — synthetic/regression-fixture data, not a live client audit.
2. **There is currently no mechanism anywhere in this platform for a client model to even be evaluated.** `docs/tickets/README.md`'s validation-service section lists `VS-017` ("client-supplied prediction baseline") as **deferred, not built** (Sprint 03's own deferral list: "VS-013–VS-017 deferred"). Until VS-017 ships, `validation-service` has no code path that ingests a client's predictions and produces a DM verdict against them at all — only Naive0 vs. NaiveLast.

This means the situation is stricter than "no model has passed the gate yet": **there is currently no gate to fail or pass**, because the capability the gate would check (a real client-model DM verdict) does not exist upstream. Any "real" eligibility check this backlog builds is therefore checking against a contract that `validation-service` doesn't yet implement — which is exactly why story ECON-004 makes mocking that upstream response, and never calling the real `validation-service` API, a hard architectural rule, not an implementation convenience.

**My own assessment, stated plainly rather than suppressed**: the ethical-boundary case for staying *fully* deferred (not even scaffolding) is a legitimate position, and I considered it before writing this backlog. I did not adopt it, for two reasons specific to what's actually authorized below: (1) every story here produces zero externally-observable capability — no endpoint in this scope can ever return a profitability number, by construction, because no real integration exists to feed it a real "eligible" result; the artifact this backlog produces is inert until two separate, independently-gated future events occur (VS-017 shipping *and* a real client model demonstrating stable outperformance). (2) The user has explicit authority to approve scaffolding-only work and did so explicitly, distinct from silently proceeding past the trigger. If either of those two conditions weren't true — if any story here could produce a reachable code path returning a number, or if this were being done without the user's explicit, informed sign-off on the ethical distinction — I would refuse to write it. I flag this here rather than deciding it unilaterally: **the requester should treat this paragraph as the thing to re-confirm, not the backlog's existence.**

## Explicit scope/dependency decisions

1. Trigger override recorded above, with the finding that VS-017 (the precondition for a real client-model DM verdict to even exist) is itself unbuilt — this is disclosed to the requester, not assumed away.
2. `economic.*` schema stores cost/slippage/turnover **assumption and simulation-input records** (fee schedules, slippage models, simulation configs) — never a table of computed profitability *outputs* tied to any real run, since no real run has earned the right to be simulated against yet (ECON-002's own design decision, stated again there).
3. Contracts (ECON-003) are kept **local** to `economic-service` for this backlog, not added to `libs/common`. Recommendation, not a final call (Tech Lead/dev-agent's to confirm): `libs/common`'s own DRY rule (implementation-plan.md section 9) is "extract on second duplication, cross-module goes to libs/* only when genuinely shared" — today exactly one module (`economic-service`) needs these shapes; `reporting-service` and `dashboard-web` don't render economic simulations yet (their own triggers, #7/#8, are about audit reports and run/split viewing, not economic output). Promoting to `libs/common` now would be speculative generality against a consumer that doesn't exist.
4. Mocked-upstream-only is a hard architectural rule for this backlog (ECON-004), not a temporary stub to be quietly upgraded by a later, differently-scoped ticket without its own explicit re-authorization.
5. The structural gate (ECON-005) is this backlog's actual deliverable; every other story exists to give it something to sit inside.

## Stories

### ECON-001 — Service scaffolding [Must]
**As** the Tech Lead standing up `economic-service`, **I want** a `uv`-managed FastAPI skeleton matching this repo's established per-service shape (`src/app/{routers,dependencies,repositories}`), **so that** the structural gate (ECON-005) and everything it depends on has a real, testable place to live.

Acceptance criteria:
- [ ] `services/economic-service/pyproject.toml` (uv-managed) declares FastAPI + test deps; no import of another service's code (implementation-plan.md section 9: "no service imports another service's code").
- [ ] `src/app/` skeleton: `routers/`, `dependencies/`, `repositories/`, mirroring `validation-service`'s/`gateway-api`'s established layout.
- [ ] `GET /health` exists, following the OPS-005-01/02 precedent (real, cheap connectivity check against whatever this service's own DB dependency is, once ECON-002 exists; a hardcoded-`ok` placeholder is acceptable only until ECON-002 lands, and must be replaced in the same PR that adds the DB dependency, not left as permanent dead code).
- [ ] `tests/` with a working pytest config; `uv run pytest` passes with a non-empty, real (not vacuous) test collected.
- [ ] `README.md` status line updated from "deferred" to "scaffolded (trigger #11 not fired — see backlog override note)" — the deferred framing is not deleted, it's updated to reflect real state.

Rationale for priority: nothing else in this backlog can exist without a skeleton; matches this repo's own "scaffolding first" precedent (NFE-001/VS-001/GW-001/DASH-001).
Depends on: none

### ECON-002 — `economic.*` Postgres schema + Repository pattern for cost/slippage *inputs* [Must]
**As** `economic-service`'s own data-access layer, **I want** a schema and Repository interface for transaction-cost/slippage-model *assumption records* and *simulation configuration records* — never a table of computed profitability results tied to a real run — **so that** the schema itself cannot be used to store or retrieve a fabricated or premature profitability number, and so that schema-per-service (implementation-plan.md section 5) is respected from the first migration.

Design decision, stated explicitly per the prompt's requirement: this schema owns **inputs to a simulation** (fee tiers, slippage curve parameters, turnover assumptions, a `simulation_configs` record referencing which upstream run it would apply to) — it does **not** own an `economic_results`/`pnl`/`returns` table. A results table implies persisted profitability numbers exist to query, which begs the exact question this backlog exists to gate. If/when the gate (ECON-005) is ever satisfied for real, persisting a computed result is a new, separately-authorized story against this same schema — not assumed here.

Acceptance criteria:
- [ ] `economic` Postgres schema defined (SQLAlchemy models + Alembic migration, schema-qualified per `validation-service`'s `version_table_schema` precedent from the INF-005 finding — avoids the same cross-service `alembic_version` collision).
- [ ] Tables: `fee_schedules` (tenant_id, exchange/venue label, fee tiers), `slippage_models` (tenant_id, model kind, parameters), `simulation_configs` (tenant_id, references an upstream `validation_run_id` by opaque string/UUID — **not** a foreign key into `validation-service`'s schema, per "no service reads another service's DB schema directly," implementation-plan.md section 2). No table stores a computed return/P&L/profitability figure.
- [ ] Every table has `tenant_id` and is designed for the same RLS pattern `validation-service`/`gateway-api` already use (policy authored even if, per this repo's own precedent, enforcement against real non-superuser credentials is a later infra ticket — don't re-litigate INF-014's sequencing here, just don't regress it).
- [ ] `EconomicInputRepository`-style interfaces defined as `typing.Protocol` (mirroring VS-003's decision: no ABC, tenant_id-first parameter, plain dataclass record types, zero `sqlalchemy` import in the interface module) with an interim SQLite or Postgres-only implementation (implementer's choice, documented — this backlog doesn't require dual-backend support on day one the way `validation-service` grew into it).
- [ ] Tests: repository round-trip for each table, tenant-isolation test (two tenants, cross-tenant read returns nothing), and a schema-level test asserting no column/table name in `economic.*` matches a profitability-output naming pattern (e.g. `pnl`, `net_return`, `profit`) — a cheap, permanent regression guard against this design decision quietly eroding later.

Rationale for priority: this module's stated bounded context (implementation-plan.md section 2: "owns `economic.*` schema") is meaningless without it, and the "inputs not outputs" decision is itself part of the ethical boundary this backlog protects — Must, not Should, because deferring it risks a later ticket adding a results table without this same scrutiny.
Depends on: ECON-001

### ECON-003 — Contracts: cost/slippage parameters + simulation request shape [Must]
**As** `economic-service`'s API layer, **I want** Pydantic models for transaction-cost/slippage parameters and a simulation *request*, **so that** ECON-005's endpoint(s) have a typed, validated request shape and a typed *refusal* response shape that is structurally incapable of carrying a numeric profitability field.

Acceptance criteria:
- [ ] `SimulationRequest` Pydantic model: references an upstream run identifier, a `fee_schedule_id`/`slippage_model_id` (or inline parameters), and whatever config a simulation needs — no profitability field on the request (it's an input, not an echo).
- [ ] Two distinct, non-overlapping response models: `EligibleSimulationResult` (has numeric fields: cost-adjusted return, slippage-adjusted return, etc. — only ever constructible on the success path) and `NotEligibleForSimulation` (has **no** numeric return/profitability field at all — only a reason code/message and, where available, the upstream verdict that caused the refusal). This is enforced by them being genuinely separate Pydantic classes with disjoint field sets, not one class with optional/nullable numeric fields defaulting to `None`/`0` — a nullable-field design would let a serialization bug silently emit a `0.0` that reads as "no profit" instead of "not eligible," which is exactly the fabricated/placeholder-number failure mode this backlog exists to prevent.
- [ ] Kept local to `economic-service` (decision 3 above) — not added to `libs/common` in this backlog.
- [ ] Tests: `EligibleSimulationResult` and `NotEligibleForSimulation` fail Pydantic validation if constructed with the other's fields; a round-trip JSON-schema test confirms the two response types are distinguishable by shape alone (e.g. an API consumer's static type check can't accidentally read a profitability field off a refusal).

Rationale for priority: ECON-005's negative-test acceptance criterion (the backlog's own most important item) is only meaningful if the refusal type is structurally incapable of carrying a fabricated number — Must, load-bearing for ECON-005.
Depends on: ECON-001

### ECON-004 — Mocked upstream integration only [Must]
**As** `economic-service`, **I want** my only upstream "validation result" source in this PoC to be a local fixture explicitly labeled as fabricated-for-testing, never a real HTTP call to `validation-service`, **so that** this scaffold's plumbing can be exercised without implying — even accidentally, even in a demo — that a real model has ever beaten naive on this platform.

Acceptance criteria:
- [ ] A `MockValidationResultClient` (or equivalent) implements whatever `UpstreamValidationResult` interface ECON-005 depends on, returning **hardcoded fixture data**, never an `httpx` call to `validation-service`'s real base URL. No `VALIDATION_SERVICE_URL`-style env var is read or wired to a real network call anywhere in this backlog's code.
- [ ] The fixture module's docstring and the returned object's own repr/serialization make it unambiguous the data is fabricated (e.g. a `source: Literal["mock_fixture"]` field, never `"live"`) — this field is exactly what ECON-005's gate checks to refuse it.
- [ ] `services/economic-service/README.md` states, in its own words, the fact confirmed in this backlog's override note: `validation-service` has never produced a real "client model beat naive" result (no VS-017), so there is nothing real to integrate against yet — and states as a **hard rule for future tickets**: no story may point this client at `validation-service`'s real API without (a) VS-017 shipping and (b) a real run producing a genuine outperformance verdict, and even then, wiring the real client is its own new, explicitly authorized ticket — not a quiet edit to this one.
- [ ] Tests: a test asserting the mock client is the *only* thing implementing the upstream interface anywhere in this codebase's dependency-injection wiring (e.g. grep/introspection-based, mirroring `validation-service`'s VS-016 doc-sync-check precedent for "prove a structural fact by introspecting the real app, not by trusting a comment").

Rationale for priority: without this, ECON-005 has nothing to hand the gate, and the risk of someone "helpfully" pointing this at a real URL later is exactly the kind of quiet erosion the override note flags — Must, and its own hard rule is deliberately over-specified to make later drift a visible diff, not a silent one.
Depends on: ECON-001, ECON-003

### ECON-005 — The structural eligibility gate [Must]
**As** the validation engine's ethical boundary made real in code (not just documented), **I want** every endpoint capable of returning a computed profitability/return/net-P&L figure to run a runtime guard that refuses unless the upstream result it was given is both real (not `ECON-004`'s mock fixture) and already a documented Naive0-beating verdict per `validation-service`'s own DM-test output, **so that** it is structurally impossible for this scaffold to be extended into a real profitability claim by anyone who doesn't also re-authorize both preconditions.

This is the backlog's actual point. Acceptance criteria are written in detail, matching the "AC ties back to a verifiable behavior" bar this repo already applies to `naive_first_engine`'s own DM-test stories.

Acceptance criteria:
- [ ] One `POST /simulations` (or equivalent) endpoint exists, taking `ECON-003`'s `SimulationRequest`, and internally resolves an `UpstreamValidationResult` (via `ECON-004`'s mock client, per that story's hard rule) before doing anything else.
- [ ] A single, named guard function (e.g. `check_economic_eligibility(result: UpstreamValidationResult) -> EligibilityDecision`) is the **only** code path that may authorize calling the profitability-computation function. No route handler calls the computation function directly — it always goes through the guard first. This is checked structurally, not just behaviorally: a test that inspects the route handler's call graph (or, at minimum, asserts the computation function raises/is unreachable if invoked with a guard-refused input) proves this, not merely a happy-path integration test.
- [ ] The guard's refusal condition is the conjunction of two independently-checked facts, both required: (1) `result.source == "live"` (never `"mock_fixture"` — see ECON-004), and (2) `result.dm_verdict` indicates the client model beat Naive0 with statistical significance, per `validation-service`'s own DM-test output shape (`dm_statistic`/`dm_pvalue`/`dm_verdict`, Harvey-corrected per NFE-012/da-tese-ao-produto.md section 1.2 — the gate reads this verdict, it never recomputes or second-guesses the DM test itself, matching "no arbitrary code execution/duplication of validation logic" from this service's own README "does not own" boundary).
- [ ] On refusal, the endpoint returns a `4xx` (409 Conflict is the specific recommendation — the request is well-formed but the resource state doesn't permit the action — 422 is acceptable if the implementer documents why) with `ECON-003`'s `NotEligibleForSimulation` body: an explicit "not eligible for economic simulation" reason (distinguishing, where useful, "no real upstream result" from "upstream result exists but did not beat naive"), and **never** a `200` with a zero/null/placeholder numeric field.
- [ ] On success (both conditions true), the endpoint returns `200` with `EligibleSimulationResult`, computed by a pure function (Strategy-pattern-shaped per implementation-plan.md section 7, swappable cost/slippage models) that is only reachable through the guard.
- [ ] **The negative test is the single most important acceptance criterion in this entire backlog** — it must exist, and it must prove refusal, not merely assert an endpoint returns *some* 4xx:
  - [ ] Test 1: `ECON-004`'s real mock fixture (`source="mock_fixture"`) submitted → `409`/`422` `NotEligibleForSimulation`, response body contains no numeric field (asserted by iterating the actual parsed response JSON's keys/types, not just checking the status code).
  - [ ] Test 2: a hand-constructed `UpstreamValidationResult` with `source="live"` but a DM verdict indicating the model did **not** beat Naive0 (e.g. `dm_verdict="naive0_better"` or `p >= 0.05`) → same refusal shape as Test 1.
  - [ ] Test 3 (positive control, proving the gate isn't just always-refusing by accident): a hand-constructed `UpstreamValidationResult` with `source="live"` **and** a DM verdict indicating a real, significant, Harvey-corrected outperformance → `200` `EligibleSimulationResult` with a real computed numeric value. This case is only reachable via direct construction inside the test suite in this backlog's scope (per ECON-004, no code path in the running service can produce `source="live"` today) — the test docstring must say so explicitly, so a future reader doesn't mistake this for evidence the real integration exists.
  - [ ] Test 4: confirms that, as currently wired end-to-end through `ECON-004`'s mock-only client, **every real HTTP call to `POST /simulations` in this service, as actually deployed, returns the refusal shape** — i.e. the positive path (Test 3) is proven reachable in isolation, but never reachable through the service's real running configuration in this backlog's scope. This is the test that makes the override note's core claim ("this scaffold cannot return a profitability number today, structurally") independently verifiable rather than asserted.
- [ ] README documents the gate's exact two-condition logic in prose, cross-linking this ticket, so a future contributor reads the rule before touching the guard function.

Rationale for priority: this is the entire reason this backlog exists — Must, and deliberately the most heavily specified story in this document; every other story is a prerequisite for this one to be meaningful.
Depends on: ECON-002, ECON-003, ECON-004

### ECON-006 — No profitability language outside the gated computation path [Must]
**As** the Naive-First platform's own positioning (CLAUDE.md, da-tese-ao-produto.md section 2.7), **I want** this service's README, docstrings, and any rendered/logged output to consistently frame `economic-service` as infrastructure that *will* compute cost/slippage-adjusted returns once — and only once — a real model earns that eligibility, **so that** the service's mere existence in the repo never implies the platform currently has a profitable model, independent of whether ECON-005's gate is ever satisfied.

Acceptance criteria:
- [ ] `services/economic-service/README.md` rewritten (not just status-line-updated) to state the framing above explicitly, matching CLAUDE.md's own required language ("statistical accuracy ≠ economic value," "never imply price prediction or trading signals").
- [ ] Grep-style check (can piggyback on `ECON-005`'s tests or be its own small script, mirroring NFE-018/VS-016/LC-005's doc-sync-check precedent) confirming no docstring, log message, or README line outside `ECON-005`'s own gated success-path code contains language implying current profitability (e.g. flags occurrences of "profitable"/"beats the market"/"alpha" outside the one narrowly-scoped module that computes `EligibleSimulationResult`).
- [ ] OpenAPI-level description strings on `POST /simulations` (FastAPI's `summary`/`description` fields, which render in `/docs`) state the eligibility precondition in plain language, not just the 409 status code — a caller reading `/docs` before calling the endpoint should already understand why they'll likely get refused.

Rationale for priority: matches this repo's own non-negotiable positioning rule (CLAUDE.md) — Must, cheap, and closes the one gap ECON-005's code-level gate doesn't cover (prose can imply profitability even when no endpoint can compute it).
Depends on: ECON-001, ECON-005

### ECON-007 — Won't: real `validation-service` integration [Won't, this backlog]
Not proposed. Requires (a) VS-017 shipping in `validation-service` (currently deferred, unbuilt) and (b) a real client model producing a genuine outperformance verdict — neither exists. Revisiting this is its own future, explicitly-authorized ticket per ECON-004's hard rule, not a natural extension of this backlog.

### ECON-008 — Won't: UI / `dashboard-web` integration [Won't, this backlog]
Not proposed. `dashboard-web`'s own backlog (see `docs/product/backlog-dashboard-web.md`) doesn't reach economic simulation views — a UI surface for a service that cannot yet return a real number would itself risk implying eligibility that doesn't exist. Future, not-yet-authorized item.

### ECON-009 — Won't: real report/output distribution [Won't, this backlog]
Not proposed. `reporting-service` (trigger #7) doesn't render economic-simulation output, and this backlog produces no artifact that should be distributed to a client — the gate's entire purpose is that nothing here is client-facing yet.

### ECON-010 — Won't: `economic_results`/persisted profitability output table [Won't, this backlog]
Not proposed, and deliberately called out separately from ECON-002's own scope decision: persisting a computed result implies one has legitimately been computed. If ECON-005's gate is ever satisfied for real, adding a results table is a new, separately-authorized story against `economic.*`, reviewed with the same scrutiny as ECON-005 — not an incidental addition to a later "add persistence" ticket.

### ECON-011 — Won't: JWT/session auth beyond scaffold-minimum [Won't, this backlog]
Not proposed. Matches `dashboard-web`'s own DASH-107 precedent — GW-006-style API-key auth (if any auth is even needed to exercise this PoC locally) is sufficient; a full auth build-out is out of scope for a service that returns no real data yet.

## Backtesting / historical simulation (new)

Source (this section): the six stories above (`ECON-001`–`ECON-006`, all done, Sprint 13, verified per `docs/tickets/README.md`'s `services/economic-service (ECON-*)` section and `docs/sprints/sprint-13.md`'s Outcome), `services/economic-service/README.md` as currently built (read in full before writing this section — its "The structural eligibility gate (ECON-005)" and "No profitability language outside the gate (ECON-006)" sections in particular), and the same override note and hard constraints stated above, unchanged, not re-litigated.

**What was explicitly requested and declined, stated here so it isn't re-derived from a later diff**: a full automated trading system — buy/sell automation against a real exchange (Binance, crypto.com), risk-tiered users, "win in return" framing, with an explicit stated intent to later switch from fictional to real trading. That is declined outright, not narrowed into this backlog in any form. It directly contradicts CLAUDE.md's core finding (no model beat naive in a stable, significant way; best directional accuracy 52.51%, barely above chance) and its non-negotiable positioning rule. What follows is a narrow, on-demand, developer/analyst-facing extension of the *existing* simulation-only posture `ECON-005` already built — running the identical gated computation across a *set* of historical run ids instead of one — nothing more.

**This section changes nothing about the gate itself.** Every story below is additive scaffolding around `ECON-005`'s already-built `check_economic_eligibility` function. It introduces no new eligibility logic, no new upstream integration, and — per the hard constraints below — no exchange integration of any kind. Per `ECON-004`'s still-binding hard rule, `MockValidationResultClient` remains the only DI-wired upstream client; **this means every story in this section is, exactly like `ECON-001`–`ECON-006` before it, inert scaffolding today** — no real backtest can return a real number through the real running service until `VS-017` ships in `validation-service` *and* a real run produces a genuine, Harvey-corrected, statistically significant outperformance verdict. State this plainly in every story's own documentation acceptance criterion below, not just here — a future reader must never mistake this section's existence for evidence that backtesting is currently usable.

### Hard constraints carried into every story below (non-negotiable)

1. **No real exchange integration, ever, in this backlog.** No Binance/crypto.com/any-venue API client, no order-placement logic, no wallet-connection code — mocked or otherwise — that could later be pointed at a real venue.
2. **Gated behind `ECON-005`'s existing eligibility check, unmodified.** A backtest entry can only be "eligible" against an upstream result that is real (`source == "live"`) and already shows significant, Harvey-corrected outperformance vs. Naive0. No such result exists anywhere on this platform today (per the Override note above, `VS-017` is unbuilt), so this feature is inert scaffolding today, not a working tool — stated explicitly in `ECON-014` below, not left implicit.
3. **No "automatic," "robo," "risk tier," or "invest and win" language anywhere.** This is a single-run, on-demand backtest a technical user or auditor triggers manually to inspect one specific historical scenario — never a recurring or automated process, never a decision made on anyone's behalf.
4. **Framing.** Every story's README/API-doc language states this computes a hypothetical, retrospective, cost/slippage-adjusted result for research purposes — never "returns," "profit," or "win" in a forward-looking or promotional sense.
5. **No custody of real or simulated user funds, no real wallet connection, nothing resembling a retail-facing financial product.** Developer/analyst tooling only.

### ECON-012 — Batch backtest endpoint: the existing gate applied across a set of historical run ids [Should]
**As** `economic-service`'s API layer, **I want** a `POST /backtests` endpoint that accepts a bounded list of upstream run ids and applies `ECON-005`'s exact, unmodified `check_economic_eligibility` guard independently to each one, **so that** a technical user or auditor can inspect, in one on-demand manual call, which of several historical runs would have been eligible for cost/slippage-adjusted computation — never as a recurring or automatic process.

Acceptance criteria:
- [ ] `BacktestRequest` Pydantic model: `run_ids: list[str]`, bounded (e.g. `max_length=50`, `422` if exceeded) — a hard ceiling that keeps this a bounded, one-shot manual call, never a batch-processing pipeline — plus `fee_schedule_id`/`slippage_model_id` (shared across the set; implementer documents if per-run overrides are supported, not required this story).
- [ ] The endpoint resolves an `UpstreamValidationResult` per run id via the exact same `Depends(get_upstream_client)` DI seam `ECON-004`/`ECON-005` already use — this story adds zero new upstream-integration code, confirmed by a test asserting no second implementer of `UpstreamValidationResultClient` exists (extends `ECON-004`'s own `test_mock_client_is_the_only_di_wired_implementation` rather than duplicating it).
- [ ] Each run id is passed through `check_economic_eligibility` (imported, not reimplemented) — a test asserts the batch handler calls that exact function object, not a parallel copy of its logic.
- [ ] Response is `BacktestResponse`: an ordered list of per-run entries, each either `EligibleSimulationResult` or `NotEligibleForSimulation` (both from `ECON-003`, unmodified), each tagged with its originating `run_id` — same disjoint-shape discipline as `ECON-003`, verified by the same style of JSON-schema-shape test proving the two entry kinds are distinguishable by shape alone.
- [ ] One run id's refusal never blocks another's evaluation — partial, independently-gated results are returned for a well-formed request; a malformed or duplicate run id is a `422` at request-validation time, not folded into the per-run result list.
- [ ] Tests, batched equivalents of `ECON-005`'s four required cases: (1) an all-mock-fixture batch → every entry refused; (2) a batch with a hand-constructed `source="live"` entry that still fails the significance check → refused; (3) positive control: a batch containing one hand-constructed, in-test-only `source="live"`-and-significant entry alongside refused ones → proves entries are gated independently, not all-or-nothing; (4) confirms that, as currently wired end-to-end (`ECON-004`'s mock-only client), **every real call to `POST /backtests` through the real running service returns an all-refused batch** — this is Test 4's batched equivalent and is exactly as load-bearing here as it was in `ECON-005`; its docstring must say so explicitly.
- [ ] OpenAPI `summary`/`description` state plainly this is a manual, on-demand, single-request trigger for retrospective inspection of a fixed set of run ids — no scheduling, no recurring execution, no automatic-triggering language anywhere in the rendered `/docs`.

Rationale for priority: Should, not Must — this is a batch/UX convenience layered over an already-built, already-verified gate (`ECON-005`), not new eligibility capability, and since no eligible input can exist today (no `VS-017`, no live outperformance result), there is no urgency behind it; matches this module's own "scaffolding stays inert until the real gate can be satisfied" posture.
Depends on: ECON-003, ECON-004, ECON-005.

### ECON-013 — Persist backtest results, eligible-only, tagged retrospective [Should]
**As** `economic-service`'s data-access layer, **I want** a new `economic.backtest_results` table that stores exactly and only the rows produced by `ECON-012`'s eligible branch, tagged with a fixed `result_kind="retrospective_backtest"` marker, **so that** a client can retrieve a backtest's results later without recomputation, while preserving `ECON-002`'s own "never store a fabricated or premature profitability number" principle — refused entries are never persisted, because there is nothing legitimate to persist for them.

Acceptance criteria:
- [ ] New table `backtest_results` (`tenant_id`, `backtest_id`, `run_id`, `fee_schedule_id`, `slippage_model_id`, `cost_adjusted_return`, `slippage_adjusted_return`, `total_cost_bps`, `upstream_dm_statistic`, `upstream_dm_pvalue`, `upstream_dm_verdict`, `computed_at`, `result_kind: Literal["retrospective_backtest"]` fixed-value column) — every row corresponds 1:1 to one `ECON-012` eligible-branch entry; there is no row shape or column for a refused entry (nothing to store — a refusal produces no numeric fact worth persisting, matching `ECON-002`'s own design decision that this schema stores inputs/earned-outputs only, never a placeholder).
- [ ] The insert path is reachable **exclusively** from inside `ECON-012`'s eligible branch — mirroring `ECON-005`'s "only reachable through the guard" structural discipline — proven by a test that no other function/route in the service can construct a `backtest_results` row (an unreachable-without-the-eligible-branch test, not merely a happy-path insert test).
- [ ] A schema-level regression test extends (does not duplicate) `ECON-002`'s `test_no_profitability_columns.py` pattern: confirms no column anywhere in `economic.*`, including this new table, matches a live/forward-looking claim (`profit`, `win`, `expected_return`, `forecast_*`) — only retrospective/already-computed naming is present.
- [ ] Repository interface (`BacktestResultRepository` or equivalent) is a `typing.Protocol`, `tenant_id`-first, zero `sqlalchemy` import in the interface module — same shape as `ECON-002`'s `EconomicInputRepository`, not a new pattern.
- [ ] `README.md`'s new Backtesting section (see `ECON-014`) states explicitly that this table can, and today does, contain zero rows, and will continue to for as long as `ECON-004`'s mock-only rule holds — its mere existence is schema readiness, not evidence of any stored finding.

Rationale for priority: Should — only meaningful once `ECON-012` exists and once a client actually wants durable results instead of a same-call response; not Must, since nothing can populate a real row today and re-computation-per-call is an acceptable interim behavior for inert scaffolding.
Depends on: ECON-002, ECON-012.

### ECON-014 — Documentation and doc-sync extension: backtesting is inert until real data exists [Must]
**As** the platform's own positioning (CLAUDE.md, `da-tese-ao-produto.md` section 2.7), **I want** `economic-service`'s README and rendered API docs to state, as unmissably as `ECON-004`/`ECON-006` already do for the single-run gate, that batch backtesting runs the identical inert scaffold across many historical runs — not a working analysis tool today — and **I want** `ECON-006`'s `check_profitability_language.py` doc-sync check extended to scan every new file this section adds, **so that** this extension cannot become the platform's first quiet leak of forward-looking profitability language.

Acceptance criteria:
- [ ] `README.md` gains a "Backtesting (ECON-012/013)" section, structured like the existing ECON-004/ECON-005/ECON-006 sections, stating explicitly: (a) this is a single-run, on-demand, manually-triggered inspection of a caller-supplied list of historical run ids, never a recurring or automated process; (b) it is gated by `ECON-005`'s exact, unmodified `check_economic_eligibility` function, reused, not reimplemented; (c) it is inert today for the same two reasons the rest of the service is (`VS-017` unbuilt, no live outperformance result has ever existed on this platform); (d) no exchange integration, no real or simulated custody of funds, and no user-facing investment product exists or is implied anywhere in this feature.
- [ ] `scripts/check_profitability_language.py`'s scanned-file set is extended to include this section's new files — named explicitly in this story: the batch router (`ECON-012`), the batch request/response contracts (`ECON-012`), and the backtest-results model/repository files (`ECON-013`) — verified by a test that the check fails when a forbidden word is injected into one of the new files and passes once removed, mirroring `ECON-006`'s own drift-detection precedent exactly (not a new mechanism).
- [ ] `POST /backtests`'s OpenAPI `summary`/`description` use "hypothetical," "retrospective," and "cost/slippage-adjusted result for research purposes" language, never "returns," "profit," or "win" in a promotional or forward-looking sense — confirmed by scanning the actual generated `/openapi.json` output, not just the docstring source (same discipline `ECON-006` already applied to `POST /simulations`).

Rationale for priority: Must — cheap, and closes the one gap none of `ECON-012`/`ECON-013`'s own acceptance criteria structurally cover (prose can imply capability or profitability even when no endpoint can compute a real number); directly answers the concern that motivated scoping this narrowly in the first place.
Depends on: ECON-006, ECON-012.

### ECON-015 — Won't: real exchange integration [Won't, this backlog]
Not proposed, and never will be extended into from this backlog. No Binance/crypto.com/any-venue API client, no order-placement logic, and no wallet-connection code of any kind — mocked or real — that could later be pointed at a real trading venue. This is the core of what was explicitly requested and explicitly declined before this backlog was authored; recorded here so it reads as a deliberate boundary, not an oversight a future contributor might "helpfully" close.

### ECON-016 — Won't: automated, recurring, or "robo" execution [Won't, this backlog]
Not proposed. Every backtest in this section (`ECON-012`) is a single, on-demand, manually-triggered call against a fixed, caller-supplied list of run ids in the request body — no scheduler, no background job, no daemon, no concept of "run this backtest automatically every day" anywhere in this backlog or the code it authorizes.

### ECON-017 — Won't: risk tiering or user-facing "investment" framing [Won't, this backlog]
Not proposed. No user-risk-tier concept, no "invest and win" copy, no signup flow through which a retail user could hand this platform money or a wallet connection. This remains developer/analyst tooling only, triggered by a technical user or auditor inspecting one specific historical scenario at a time — matches ECON-011's own precedent of declining auth/product build-out beyond scaffold-minimum.

### ECON-018 — Won't (explicitly considered and declined): mocked-wallet bookkeeping data structure [Won't, this backlog]
Explicitly considered, per the requester's own framing that a *pure bookkeeping* data structure (starting balance, position size over simulated time — a plain record, not an integration surface) is conceptually distinguishable from exchange-integration scaffolding. Declined for this backlog anyway: even a purely internal, no-I/O data structure risks reading, to a future contributor, as the first piece of exchange-integration scaffolding — especially once it exists alongside `ECON-012`'s run-id-driven batch iteration, which is exactly the shape a later "wire this to a real venue" ticket would reuse. If a future ticket genuinely needs this, it should be proposed and reviewed on its own terms, with the "pure bookkeeping, not integration surface" distinction re-stated and re-confirmed at that time by whoever authorizes it — not introduced quietly inside this narrower extension.
