# NFE-007 — Error metrics: MAE, RMSE

**Status: done**

## Implementation notes
`mae`/`rmse` added to `metrics.py`, typed on `pd.Series`, raise `ValueError` via a shared `_check_aligned` helper on length mismatch or index mismatch (no auto-align, no truncation). Convention documented in the module docstring.

Shared synthetic 1h fixture created at `libs/naive_first_engine/tests/fixtures.py` (NFE-005 didn't exist yet when this ticket ran, so this ticket originated it) — `build_1h_reference_scenario()` returns `y_true`/`y_pred_naive0`/`y_pred_ols` as aligned `pd.Series`, calibrated by an affine rescale of a seeded `Gamma(shape=0.8)` sample so `mae()`/`rmse()` against it reproduce the published 1h numbers to float64 precision (well under `TOLERANCE = 1e-6`, itself defined as a named constant in `fixtures.py`, not inlined as a magic number). Module docstring in `fixtures.py` explicitly states this is a synthetic fixture, not real BTC data. **NFE-005/009/011/015 must import from this module** (`build_1h_reference_scenario`, `NAIVE0_MAE_1H`, `NAIVE0_RMSE_1H`, `OLS_MAE_1H`, `OLS_RMSE_1H`, `TOLERANCE`) instead of inventing a second synthetic 1h series.

Test run (`libs/naive_first_engine`, `.venv\Scripts\python.exe -m pytest tests/test_metrics.py -q`):
```
........                                                                 [100%]
8 passed in 0.42s
```

## Analysis
Story: NFE-007 (Must), depends on NFE-001 only — independent of the baselines branch (NFE-004/005/006), can run in parallel with it. Constraining docs: da-tese-ao-produto.md section 1.4.1 ("Naive0 venceu em MAE/RMSE médios" — the paper's headline conclusion) and section 1.3 (1h Naive0 MAE 0.003627/RMSE 0.005321, OLS MAE 0.003683/RMSE 0.005367 are the regression targets).

## Design
No new pattern (pure functions). File touched: `libs/naive_first_engine/src/naive_first_engine/metrics.py` only, plus `tests/test_metrics.py` (new file). DRY check: grep `metrics.py` first — docstring only currently.

`mae(y_true, y_pred) -> float`, `rmse(y_true, y_pred) -> float`, both typed on `pd.Series` (or `Sequence[float]` — pick one convention and keep it consistent across every function added to `metrics.py` in this sprint, since NFE-008/009/010 extend the same file; document the chosen convention in the module docstring so later tickets don't diverge).

## Implementation acceptance criteria
- [x] `mae(y_true, y_pred) -> float` and `rmse(y_true, y_pred) -> float` exist, typed.
- [x] Both raise (not silently truncate/misalign) on mismatched-length or misaligned-index inputs.

## Test acceptance criteria
- [x] Regression test reproduces da-tese-ao-produto.md 1h Naive0 (MAE 0.003627, RMSE 0.005321) and OLS (MAE 0.003683, RMSE 0.005367) within documented tolerance, on a reconstructed reference scenario. NFE-005 had not been implemented yet at the time this ticket ran (grepped `tests/` first, confirmed no fixture existed), so this ticket originates the shared fixture in `tests/fixtures.py` for NFE-005/009/011/015 to reuse, rather than the other way around.
- [x] Unit test confirms `mae`/`rmse` raise on mismatched-length or misaligned-index inputs.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_metrics.py -q`, confirm pass, paste output.

## Review acceptance criteria
- Tech Lead verifies: tolerance is documented as a constant/comment, not a magic number (`TOLERANCE = 1e-6` in `tests/fixtures.py`); the shared fixture module (`tests/fixtures.py`) is the single source of the 1h Naive0/OLS numbers — NFE-005 must import it, not derive its own, to avoid drift.

## Documentation acceptance criteria
- [x] `metrics.py` module docstring states the `y_true`/`y_pred` type convention chosen here, for NFE-008/009/010 to follow.
