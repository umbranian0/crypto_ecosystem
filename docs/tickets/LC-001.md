# LC-001 — Package scaffolding

**Status: done** (scaffolded directly by Tech Lead, same precedent as NFE-001/VS-001 — pure scaffolding, no design decision to delegate)

## Analysis
Story: LC-001 (Must), depends on none — literal first step for `libs/common`, same precedent as NFE-001/VS-001. Constraining docs: docs/product/backlog-libs-common.md LC-001 acceptance criteria; docs/implementation-plan.md section 3 (repo layout: `libs/common/src/naive_first_common/`), section 9 (uv package manager, tests before API). Binding sprint note (sprint-04.md): this ticket must **not** modify `services/validation-service/pyproject.toml` — that wiring is VS-010's job, keeping the two backlogs independently shippable.

## Design
Pattern: none yet (pure scaffolding — no logic to apply a pattern to). Files: `libs/common/pyproject.toml` (new), `libs/common/src/naive_first_common/__init__.py` (new, empty public API for now), `libs/common/tests/__init__.py` + a `pytest` config that collects zero tests as a baseline (new), `libs/common/README.md` (edit — status line only).
DRY check: `libs/common/` currently contains only a README (no `src/`/`tests/` yet) — nothing to deduplicate. Mirror `libs/naive_first_engine/pyproject.toml`'s shape (hatchling build backend, `src/` layout, `[tool.pytest.ini_options] testpaths = ["tests"]`, `[dependency-groups] dev`) since that's this repo's established pattern for a `uv`-managed lib package, adjusted per LC-001's own dependency constraint below.

**Dependency constraint (binding, not a design choice)**: `pyproject.toml`'s `[project.dependencies]` lists **only** `pydantic>=2.6` (matching `validation-service`'s pinned floor) as a runtime dependency. `fastapi` appears **only** under `[dependency-groups] dev` (alongside `pytest`, `httpx` if needed for LC-004's test app later) — the lib exposes a `Depends()`-compatible callable, not a FastAPI app, per the README's contract ("no framework lock-in beyond Pydantic").

## Implementation acceptance criteria
- [x] `libs/common/pyproject.toml` exists, declares `naive_first_common` as a plain-Python package, `pydantic` as its only `[project.dependencies]` entry, `fastapi`/`pytest`/`httpx` under `[dependency-groups] dev` only.
- [x] `src/naive_first_common/` skeleton exists at `libs/common/src/naive_first_common/__init__.py` matching implementation-plan.md section 3's layout.
- [x] `libs/common/tests/` exists with a working `pytest` config (`testpaths = ["tests"]`); `.venv\Scripts\python.exe -m pytest` passes with zero tests collected as a baseline.
- [x] `services/validation-service/pyproject.toml` is **not** modified by this ticket.

## Test acceptance criteria
- [x] `.venv\Scripts\python.exe -m pytest -q` run inside `libs/common` after `uv venv` + `uv sync`, exits with "no tests ran in 0.01s" (pytest exit code 5, zero collected) — confirmed.

## Review acceptance criteria
- [x] **Tech Lead personally read** `libs/common/pyproject.toml`: confirmed `pydantic` is the only `[project.dependencies]` entry and `fastapi`/`httpx` are dev-group-only; confirmed `git status`-equivalent check shows no diff to `services/validation-service/pyproject.toml` from this work.

## Documentation acceptance criteria
- [x] `libs/common/README.md`'s status line updated from "planned" to "scaffolded", leaving "owns"/"does not own"/"Contract" sections as-is (full doc-sync is LC-005, deferred).
