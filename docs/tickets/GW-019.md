# GW-019 — `gateway-api` proxy: `POST /ingestion/connectors/{source}/run`

**Sprint**: 18. **Module**: `services/gateway-api`. **Status**: done. **Priority**: Must.
**Depends on**: `INGEST-008`. **Can run in parallel with**: `GW-020` (separate router file).

## Analysis
Story: backlog `GW-019`, unchanged. Enforces `implementation-plan.md`'s "`dashboard-web` calls
`gateway-api` only" rule, same precedent as `GW-016`/`GW-018`.

## Design
File: new `services/gateway-api/src/app/routers/ingestion.py` (disjoint from `runs.py`/`reports.py`).
New `INGESTION_SERVICE_URL` env var. **DRY check**: reuses `GW-009`'s existing timeout/connection-error
handling and `GW-007`'s `build_downstream_headers` unmodified — no new transport-error pattern, no new
tenant-forwarding mechanism.

## Implementation acceptance criteria
- [x] New router forwards `POST /ingestion/connectors/{source}/run` to `ingestion-service`, carrying
  the verified tenant identity via `build_downstream_headers`.
- [x] Reuses `GW-009`'s downstream-failure-to-status-code handling unmodified.

## Test acceptance criteria
- [x] Mirrors `GW-016`'s test shape: happy path, downstream timeout, downstream connection refused.
  See `tests/test_ingestion_routing.py` (also covers 404 unknown source / 422 missing credentials
  forwarded unmodified, and the 401-before-downstream-call / X-Tenant-Id-forwarded cases).

## Review acceptance criteria (Tech Lead verifies personally)
- Confirms zero changes to `runs.py`/`reports.py` (file-collision-free, matching the `GW-016`/`GW-018`
  precedent).
- Confirms `build_downstream_headers` is reused unmodified, not reimplemented.

## Documentation acceptance criteria
- [x] `services/gateway-api/README.md`'s Routes section gains this proxy route; `INGESTION_SERVICE_URL`
  documented alongside `VALIDATION_SERVICE_URL`/`REPORTING_SERVICE_URL` (already documented there by
  GW-021, which introduced `IngestionServiceClientDep`/`INGESTION_SERVICE_URL` — this ticket reuses that
  client/env var unmodified and adds its own Contract-section entry for the new route).
