# UAT-014 — Plain-language `/help/concepts` page

**Module**: `services/dashboard-web`. **Depends on**: UAT-012 (file-ordering on `base.html`'s nav
region) and UAT-008 (file-ordering on `run_new.html`, both add markup to that form template).

## Analysis
Story: `docs/product/backlog-uat-findings.md` UAT-014. Single new static template, no rewrite of existing
tooltips (RSS-001/002, DH-008 stay as-is). Content must pass CLAUDE.md's positioning scan (no
"prediction"/"signal" language) — same discipline as every other template in this service.

## Design
Pattern: none new. Files touched:
- `services/dashboard-web/src/app/routers/*.py` (new route, likely same `runs.py` or a small new
  `help.py` router if one doesn't already exist for static pages — check existing router-per-concern
  convention first) — `GET /help/concepts`, pure static render, no downstream call.
- `services/dashboard-web/src/app/templates/help_concepts.html` — new template explaining purge gap and
  walk-forward windows in plain language.
- `base.html` — nav link to `/help/concepts`.
- `run_new.html` — link near the relevant fields (`purge_gap_hours`, `train_window`/`test_window`/`step`).
**DRY check note**: grep existing static-page routes (e.g. any `GET /about`-style route) before adding a
new router module — reuse the existing pattern for a no-downstream-call static page.

## Implementation acceptance criteria
- [x] `/help/concepts` page exists, explains purge gap and walk-forward windows in plain language.
      Closed by new `src/app/routers/help.py` (`GET /help/concepts`) + `templates/help_concepts.html`.
- [x] Linked from `base.html`'s nav and from `run_new.html`'s form near the relevant fields — "Concepts"
      nav link in `base.html`, and a link placed directly above the `purge_gap_hours` field in
      `run_new.html`.
- [x] Content contains no "prediction"/"signal"/forecast-of-future-price language (CLAUDE.md) — verified
      by `test_help_concepts_page_has_no_banned_positioning_words`.
- [x] Existing inline `data-tooltip` hints (RSS-001/002, DH-008) unchanged, not replaced — no tooltip
      markup was touched, only a new `<p><a>` link added.

## Test acceptance criteria
- [x] Test asserts `GET /help/concepts` returns 200 and renders expected content markers
      (`test_help_concepts_returns_200_and_expected_content_markers`).
- [x] Test asserts `base.html`/`run_new.html` contain a link to `/help/concepts`
      (`test_base_html_nav_links_to_help_concepts`, `test_run_new_links_to_help_concepts`).
- [x] A positioning-language scan test asserts no banned terms appear in the new page
      (`test_help_concepts_page_has_no_banned_positioning_words`, same convention as
      `test_runs_list_template_has_no_banned_positioning_words`).
- [x] Full `dashboard-web` suite re-run, zero regressions (341 passed, 7 deselected).

## Review acceptance criteria (Tech Lead)
- [x] Personally read the new page's copy against CLAUDE.md's positioning constraint — the page frames
      purge gap/walk-forward windows as validation-protocol mechanics only; an initial draft used the
      word "prediction" in a negated sentence and was reworded to avoid the banned word entirely rather
      than rely on negation (the CAVEAT_SENTENCE precedent permits only "forecast," negated, not a
      second banned word).
- [x] Confirm existing tooltips untouched — confirmed via diff: only `base.html`, `run_new.html` (one new
      `<p><a>` link, no tooltip spans touched), `main.py`, and two new files were changed.
- [x] Full suite re-run personally (341 passed, 7 deselected, zero regressions).

## Documentation acceptance criteria
- [x] `services/dashboard-web/README.md` notes the new page/route and its purpose. See "Plain-language
      concepts page (UAT-014)" section.

## Status: done
