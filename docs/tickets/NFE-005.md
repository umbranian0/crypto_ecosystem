# NFE-005 — Naive0 baseline

**Status: done**

## Analysis
Story: NFE-005 (Must), depends on NFE-004. Constraining docs: solution-design.md section 1 principle 1 ("no code path that scores a model without them"), da-tese-ao-produto.md section 2.3.1 ("benchmark naive obrigatório"), section 1.3 (1h Naive0 MAE 0.003627 is the regression target).

## Design
Pattern: Strategy implementation (concrete class fulfilling NFE-004's `Baseline`). File touched: `baselines.py` only (add `Naive0` class), plus `tests/test_baselines.py` (extend, don't duplicate — DRY check: grep for existing `Naive0`/fixture helpers first, reuse any synthetic-series fixture NFE-004's test already set up).

`Naive0.predict(train, test) -> Series`: returns a zero-valued Series indexed exactly like `test` (forward-return target is 0 by construction — a pure function of `test`'s index shape only, per the story's own framing).

## Implementation acceptance criteria
- [x] `Naive0` implements `Baseline`, returns zero-valued forecast for every test-period timestamp, aligned to forward-return target (not price level).

## Test acceptance criteria
- [x] Unit test: `Naive0.predict` never reads any column/value from `test` other than its index. (`test_naive0_predict_ignores_test_values_and_train_entirely` — same index, different values, asserts identical output.)
- [x] Regression test: computes MAE for Naive0 on a reconstructed 1h out-of-sample scenario and matches da-tese-ao-produto.md 1h Naive0 MAE (0.003627) within a documented tolerance. Reused the existing `tests/fixtures.py::build_1h_reference_scenario` (already present in the repo when this ticket was implemented) rather than inventing a new one.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_baselines.py -q`, confirm pass, paste output.

```
......                                                                   [100%]
6 passed in 0.43s
```

## Review acceptance criteria
- Tech Lead verifies: the regression fixture used to hit MAE 0.003627 is documented in-file (a comment or docstring stating it's a synthetic/documented reconstruction, not real BTC data, since the real dataset isn't in this repo — see NFE-015's ticket for the full disclosure rule this must not violate silently).

## Documentation acceptance criteria
- [x] `baselines.py` docstring kept accurate for `Naive0`. The shared fixture module `tests/fixtures.py` (with `build_1h_reference_scenario`) already existed in the repo at implementation time — this ticket did not create it, only reused it per its own docstring's instruction. Noted here again for NFE-007/009/011/015: reuse `tests/fixtures.py`, do not re-derive.
