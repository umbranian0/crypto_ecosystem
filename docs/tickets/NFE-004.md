# NFE-004 — Baseline Strategy interface

**Status: done**

## Analysis
Story: NFE-004 (Must), depends on NFE-002 (needs `Split` shape — NFE-003 will already be done too by execution order, `Split` will include purge fields; that's fine, this ticket doesn't need to touch them). Constraining docs: implementation-plan.md section 7 (Strategy pattern, named explicitly for `naive_first_engine.baselines`: "One interface (`predict(train, test) -> Series`), swappable implementations").

## Design
Pattern: **Strategy** (implementation-plan.md section 7 table, row 1 — this is the interface the pattern's row names). File touched: `libs/naive_first_engine/src/naive_first_engine/baselines.py` only, plus `tests/test_baselines.py` (new file — first content in this test file, no DRY conflict). DRY check: grep `baselines.py` first — currently docstring only.

`Baseline` as a `typing.Protocol` (fits "pure functions, zero I/O" style better than an ABC with inheritance ceremony — no framework dependency either way) with `predict(self, train: pd.Series, test: pd.Series) -> pd.Series`. The signature itself must make it structurally impossible to reach data past `train_end` — i.e. the function receives only `train` and `test` as already-sliced Series per a `Split`, never the full original series plus boundaries (that would let an implementation "reach past" its slice by accident).

## Implementation acceptance criteria
- [x] `Baseline` protocol/ABC defined in `baselines.py` with `predict(train, test) -> Series`.
- [x] Interface only accepts train/test data implied by a `Split` — no access to data outside train boundary, enforced by the signature (already-sliced inputs), not by convention/comment alone.
- [x] Docstring/type comment states implementations must not read past `train_end`, cross-referencing NFE-003's purge guarantee.

## Test acceptance criteria
- [x] Unit test: a minimal fake `Baseline` implementation can be constructed and satisfies `isinstance`/structural checks (whichever the Protocol/ABC choice implies) — confirms the interface is actually usable, not just declared.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_baselines.py -q`, confirm pass, paste output.

```
..                                                                       [100%]
2 passed in 0.42s
```

Full suite (`.venv\Scripts\python.exe -m pytest -q`):

```
...........................                                              [100%]
27 passed in 0.46s
```

## Review acceptance criteria
- Tech Lead verifies: signature exactly `predict(train, test) -> Series` (matches implementation-plan.md section 7 verbatim); no baseline-specific logic (no Naive0/NaiveLast code) leaks into this ticket — that's NFE-005/006's scope.

## Documentation acceptance criteria
- [x] `baselines.py` module docstring still accurate after the interface is added (currently states "Strategy interface and mandatory Naive0/NaiveLast" — fine, no edit needed unless wording drifts).
