# NFE-003 — Configurable purge gap between train and test

**Status: done**

## Analysis
Story: NFE-003 (Must), depends on NFE-002. Covers all 5 acceptance criteria in the backlog. Constraining docs: da-tese-ao-produto.md section 2.1 ("o protocolo de validação em si") and CLAUDE.md ("Preprocessing... never globally... this is the exact leakage failure mode the whole business case is built on avoiding") — the purge gap is the mechanism the entire product's honesty claim rests on. It must default to a value, never to "off".

## Design
No new pattern. File touched: `splitting.py` only (extends NFE-002's `generate_splits`/`Split`), plus `tests/test_splitting.py` (extends NFE-002's test file — do not create a second test file, DRY within the module). DRY check: grep `splitting.py` for the existing `Split`/`generate_splits` before editing; extend, don't duplicate.

`generate_splits(...)` gains a `purge_gap: int | pd.Timedelta` parameter with **no default of zero/off** — per the acceptance criteria it must default to *some* nonzero value (thesis default: 24h, da-tese-ao-produto.md section 1.2) while still being explicitly overridable to 0. `Split` gains `purge_start`/`purge_end`.

## Implementation acceptance criteria
- [x] `generate_splits` accepts `purge_gap`; `Split` exposes `purge_start`/`purge_end` distinct from train/test.
- [x] Purge gap has a nonzero default (thesis's 24h) — there is no code path that returns a split without purge-gap enforcement applied by omitting the parameter.
- [x] Passing `purge_gap=0` explicitly still works (adjacent train/test, no implicit forcing).

## Test acceptance criteria
- [x] Unit test: with `purge_gap > 0`, no timestamp between `train_end` and `test_start` appears in either train or test index.
- [x] Unit test: with `purge_gap=0` explicit, train and test are immediately adjacent.
- [x] Unit test: replicates the thesis's exact config (24h purge gap, 1h/6h/24h horizons) and asserts the returned gap width matches exactly for each horizon.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_splitting.py -q`, confirm pass, paste output in final report.

## Review acceptance criteria
- Tech Lead verifies by reading `generate_splits`'s signature and body directly: confirm there is no default of `purge_gap=0` and no branch that skips purge enforcement (this is the hard constraint from CLAUDE.md — treated as correctness, not style, per the dev-agent role definition).

## Documentation acceptance criteria
- [x] Module docstring confirmed still accurate ("configurable, enforced purge gap") — no edit needed.
