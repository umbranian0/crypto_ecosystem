# GW-020 — `gateway-api` proxy: `GET /ingestion/datasets`, `GET /ingestion/datasets/{source}/series`

**Sprint**: 18. **Module**: `services/gateway-api`, `libs/common`. **Status**: done (see caveat below).
**Priority**: Must. **Depends on**: `INGEST-009`. **Can run in parallel with**: `GW-019` (separate router
file).

**Caveat**: `DatasetSummaryResponse`'s "imported by both `ingestion-service`'s response model and this
proxy" wording in the Implementation acceptance criteria below is only half-satisfied — `gateway-api`'s
proxy imports the new canonical class from `naive_first_common.contracts`; `ingestion-service`'s own
`datasets.py` still defines its own local, field-for-field-equivalent `DatasetSummaryResponse` rather
than importing this one. Refactoring `ingestion-service` to depend on `naive_first_common.contracts`
was ruled out of this ticket's scope per the Tech Lead's explicit dispatch instructions (adding that new
cross-service dependency is a separate, deliberate decision, not a trivial/risk-free follow-on) — flagged
here for a follow-up ticket, not silently left undocumented.

## Analysis
Story: backlog `GW-020`, refined for `INGEST-009`'s revised `{source}/series?start=&end=&field=` shape
(not the backlog's original `{id}` sketch).

## Design
Same `ingestion.py` router file as `GW-019` — since both land in the same batch and touch the same new
file, this ticket runs **after** `GW-019`'s diff is personally verified, not concurrently (same
same-file-collision-avoidance discipline as `GW-016`/`GW-018`, Sprint 14). New `DatasetSummaryResponse`
contract class added to `naive_first_common.contracts` (reused, not hand-duplicated field lists).

## Implementation acceptance criteria
- [x] Proxies `GET /ingestion/datasets` and `GET /ingestion/datasets/{source}/series?start=&end=&field=`
  to `ingestion-service`, forwarding query params and tenant identity unchanged.
- [ ] `DatasetSummaryResponse` added to `naive_first_common.contracts` (source, earliest/latest
  timestamp, row_count), imported by both `ingestion-service`'s response model and this proxy — one
  canonical definition, per ARCH-003's convention. **Partially met**: the class is added and is
  `gateway-api`'s proxy's single canonical definition; `ingestion-service`'s own `datasets.py` still
  defines its own local copy rather than importing this one (see Caveat above — out of this ticket's
  scope per explicit dispatch instructions, not an oversight).
- [x] Reuses `GW-009`'s failure handling unmodified.

## Test acceptance criteria
- [x] Mirrors `GW-019`'s test shape for both new routes (happy path, downstream 404/400, downstream
  timeout/connection-refused, tenant-identity forwarding, 401-before-any-downstream-call) —
  `services/gateway-api/tests/test_ingestion_routing.py`.

## Review acceptance criteria (Tech Lead verifies personally)
- Confirms zero file overlap with `GW-019`'s diff (byte-for-byte identical shared-file sections before/
  after, matching the `GW-016`/`GW-018` Sprint 14 precedent).
- Confirms `DatasetSummaryResponse` is the single canonical definition (grep for a second, hand-rolled
  field list — must find none).

## Documentation acceptance criteria
- [x] `services/gateway-api/README.md`'s Routes section gains both proxy routes.
- [x] `libs/common/README.md`'s "Public API" list gains `DatasetSummaryResponse` under `contracts.py`;
  `scripts/check_doc_sync.py`/`tests/test_doc_sync.py` re-run and pass.
