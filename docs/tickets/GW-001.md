# GW-001 — Service scaffolding

**Status: done** (implemented directly by Tech Lead — pure scaffolding, no design/logic decisions to delegate, same precedent as NFE-001/VS-001/LC-001).

## Analysis
Story: GW-001 (Must), depends on nothing — literal first step. This backlog is built ahead of trigger #5 actually firing (backlog decision 1) — there is still no real pilot client; this sprint stands up the mechanism only. Constraining docs: implementation-plan.md section 3 (repo layout: `src/app/` with `routers/`, `dependencies/`, `repositories/`), section 9 (uv, tests-before-API), backlog AC (`pyproject.toml` depends on `naive_first_common`, NOT `naive_first_engine` — this service never touches the validation algorithm).

## Design
No design pattern applies (pure scaffolding). Files: `services/gateway-api/pyproject.toml`, `src/app/{__init__,main}.py`, `src/app/routers/__init__.py`, `src/app/repositories/__init__.py`, `src/app/dependencies/__init__.py`, `tests/{__init__,test_package_imports}.py`, `README.md`. DRY check: n/a, first files in the module. Mirrors `services/validation-service`'s precedent exactly (own `pyproject.toml`, `uv.sources` path dependency, no root workspace file exists in this repo).

`naive_first_common` is declared as a `uv` path/workspace dependency (`[tool.uv.sources]` pointing at `../../libs/common`, editable). `naive_first_engine` is deliberately **not** a dependency — this service routes to `validation-service`, it never imports the validation algorithm.

## Implementation acceptance criteria
- [x] `services/gateway-api/pyproject.toml` exists, declares the FastAPI app, depends on `naive_first_common` (path/workspace dependency) — not `naive_first_engine`.
- [x] `src/app/` skeleton exists (`routers/`, `dependencies/`, `repositories/` packages).
- [x] `tests/` exists with a working `pytest` config; `uv run pytest` / `.venv\Scripts\python.exe -m pytest` passes with a minimal smoke-test baseline (mirrors VS-001's rationale: a literal zero-test baseline exits pytest code 5, so a minimal import smoke test is the faithful "passing baseline").
- [x] `services/gateway-api/README.md`'s status line updated from "planned" to "scaffolded," and its opening line updated to note this backlog was built ahead of trigger #5 actually firing (decision 1) — so a future reader doesn't mistake this for "a pilot client exists."

## Test acceptance criteria
- [x] `.venv\Scripts\python.exe -m pytest -q` passes. `test_package_imports.py`: FastAPI app constructs; `naive_first_common` importable via the path dependency.

## Review acceptance criteria
- Tech Lead verified: file tree matches implementation-plan.md section 3; `pyproject.toml` depends on `naive_first_common` only (no `naive_first_engine`); `.venv\Scripts\python.exe -m pytest -q` exits 0.

## Documentation acceptance criteria
- [x] README.md status line updated to "scaffolded" plus the trigger-#5 override note.

## Environment note (same workaround as validation-service, Sprint 03/04)
`uv sync` intermittently fails to write new files in this sandboxed Windows environment (OneDrive file-creation race). Workaround used throughout this sprint: `.venv` created via `python -m venv .venv`, dependencies installed via `.venv\Scripts\python.exe -m pip install -e . -e ..\..\libs\common pytest httpx`, tests run via `.venv\Scripts\python.exe -m pytest`. Every dev agent in this sprint is instructed to use this workaround.

**Verified**: `.venv\Scripts\python.exe -m pytest -q` in `services/gateway-api` → `2 passed`.
