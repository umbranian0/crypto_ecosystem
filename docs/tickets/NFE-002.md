# NFE-002 — Rolling-origin walk-forward splitter

**Status: done**

## Analysis
Story: NFE-002 (Must), depends on NFE-001 (done). Covers all 4 acceptance criteria in the backlog. Constraining docs: implementation-plan.md section 7 (Template Method — splitter is stage 1 of the fixed order), section 3 (file = `splitting.py`), solution-design.md section 4 (`split_results` field names: `train_start`/`train_end`/`test_start`/`test_end` must be reused verbatim), da-tese-ao-produto.md section 1.3 (OLS/1h reports 53 DM splits — used as the tie-back verification number).

## Design
Pattern: none yet at this stage (Strategy/Template Method land in later tickets: NFE-004, NFE-014). File touched: `libs/naive_first_engine/src/naive_first_engine/splitting.py` only, plus `tests/test_splitting.py`. DRY check: grep `splitting.py` first — it currently only has a docstring, nothing to reuse or duplicate.

Function shape: `generate_splits(index: pd.DatetimeIndex, train_window: int | pd.Timedelta, test_window: int | pd.Timedelta, step: int | pd.Timedelta) -> list[Split]`. `Split` is a simple typed structure (dataclass) with `train_start`, `train_end`, `test_start`, `test_end` for this ticket — `purge_start`/`purge_end` are added in NFE-003 (do not add them yet, avoid scope creep into the next ticket, but do leave the dataclass easy to extend, e.g. don't freeze/finalize the schema in report_schema.py yet — that's NFE-013's job).

## Implementation acceptance criteria
- [x] `generate_splits(index, train_window, test_window, step) -> list[Split]` exists in `splitting.py`, fully typed, dataset-agnostic (no Bitcoin/price-specific logic or naming).
- [x] Each `Split` exposes `train_start`, `train_end`, `test_start`, `test_end`.
- [x] Splits are strictly non-overlapping in the forward direction and chronologically ordered.

## Test acceptance criteria
- [x] Unit test asserts the non-overlapping, chronological-order invariant on a synthetic index.
- [x] Unit test runs the splitter against a synthetic hourly series sized to reproduce the thesis's out-of-sample window (2024-01-09 to 2025-01-08, da-tese-ao-produto.md section 1.2) with a 1h horizon and asserts the split count matches 53 (cross-referenced to ARIMA 1h "0/53" in section 1.3). Window/step values documented in the test comment: full hourly index over the published window, 1-day train window, 1-day test window, 1-week step.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_splitting.py -q`, confirm pass, paste output in the final report.

## Review acceptance criteria
- Tech Lead verifies: `Split` field names match solution-design.md section 4 exactly; no purge-gap fields added prematurely; no reference to BTC/price anywhere in `splitting.py`; the 53-split reconstruction is documented (window/step values shown in a test comment) not just asserted as a magic number.

## Documentation acceptance criteria
- [x] `libs/naive_first_engine/README.md` — no structural change needed (file already listed); `Split`'s public shape is not yet part of the README's public contract section (that lands with NFE-018, deferred). Module docstring in `splitting.py` left as-is; still accurate (describes the file's eventual full scope per README, purge gap arrives in NFE-003).
