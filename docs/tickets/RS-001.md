# RS-001 — Service scaffolding (reporting-service)

Status: **done** (built directly by the Tech Lead, matching NFE-001/VS-001/LC-001/GW-001/DASH-001
precedent — pure scaffolding, no design decision to delegate)
Backlog: docs/product/backlog-reporting-service.md, RS-001 [Must]
Sprint: Sprint 12 (reporting-service PoC — docs/sprints/sprint-12.md's actual committed content is
a mismatch, containing economic-service content under a reporting-service commit title; this
ticket is sequenced per the requester's own restated sprint framing, cross-verified directly
against docs/product/backlog-reporting-service.md, which matches that framing exactly)
Depends on: none

## Analysis

`services/reporting-service` currently has only a planned-status README. Nothing else in this
sprint (RS-002 through RS-009) can be built without a `uv`-managed skeleton matching every other
service's shape (`src/app/{routers,dependencies,repositories}`, plus this service's own
`renderers/`+`templates/` for RS-003's Factory). Story maps to backlog RS-001's acceptance criteria
verbatim.

## Design

- No design pattern applies to pure scaffolding (matches NFE-001/VS-001/GW-001/DASH-001's own
  Analysis stance).
- Files touched (scoped to `services/reporting-service/` only): `pyproject.toml`, `src/app/main.py`,
  `src/app/__init__.py`, `src/app/routers/__init__.py`, `src/app/dependencies/__init__.py`,
  `src/app/repositories/__init__.py`, `src/app/renderers/__init__.py`, `src/app/templates/`
  (placeholder), `tests/__init__.py`, `Dockerfile`, `README.md`.
- **DRY check note**: grepped `services/validation-service/` and `services/gateway-api/` for the
  scaffolding shape before writing — reused the exact same `pyproject.toml` structure and the
  exact same non-root Dockerfile pattern from `services/validation-service/Dockerfile`
  (INF-003/OPS-004 precedent) — no new packaging convention invented.
- `pyproject.toml` deps: `fastapi`, `uvicorn[standard]`, `pydantic`, `sqlalchemy`, `alembic`,
  `psycopg[binary]` (RS-002), `redis` (RS-006, mirrors VS-014), `httpx` (RS-004, mirrors GW-008),
  `jinja2` (RS-003), plus `naive_first_common` as an editable path dependency
  (`../../libs/common`) — no `naive_first_engine` import (this service never runs the protocol
  itself, only renders what `validation-service` already computed).

## Implementation acceptance criteria

- [x] `services/reporting-service/pyproject.toml` declares FastAPI + `httpx` + `redis` + `jinja2` +
      `sqlalchemy`/`alembic`/`psycopg[binary]` deps; imports only `libs/common` (no other service's
      code).
- [x] `src/app/{routers,dependencies,repositories}` skeleton exists, plus `src/app/templates/` and
      `src/app/renderers/`.
- [x] `tests/` with a working pytest config; `uv run pytest` passes with 0 collected as a baseline.
- [x] `Dockerfile` matches the non-root, port-binding pattern from `validation-service`/`gateway-api`
      (not wired into `infra/docker-compose.yml` this ticket — RS-GAP).
- [x] `README.md` status moves planned -> scaffolded, with the trigger-#7 override note.

## Test acceptance criteria

- [x] `uv run pytest` runs cleanly with 0 tests collected, 0 errors — proves the package/test
      config itself is valid before any real test exists.

## Review acceptance criteria (Tech Lead personally verifies)

- [x] Confirms `pyproject.toml`'s `[tool.uv.sources]` points `naive_first_common` at
      `../../libs/common` (editable), matching every other service's convention.
- [x] Confirms the Dockerfile's non-root `USER appuser` step and `libs` build-context `COPY --from=libs`
      pattern are present (even though not yet wired into Compose).
- [x] Confirms no `naive_first_engine` dependency was added (this service never runs the protocol).

## Documentation acceptance criteria

- [x] `services/reporting-service/README.md` status line updated: planned -> scaffolded, discloses
      trigger-#7 override.

## Outcome

Built directly by the Tech Lead. `services/reporting-service/{pyproject.toml, src/app/main.py,
src/app/{routers,dependencies,repositories,renderers}/__init__.py, src/app/templates/.gitkeep,
tests/__init__.py, Dockerfile}` created; `README.md` status line updated.
