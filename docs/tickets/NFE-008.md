# NFE-008 — Error metrics: sMAPE, MASE

**Status: done**

## Analysis
Story: NFE-008 (Must), depends on NFE-001, sequenced after NFE-007 per sprint plan (not a hard data dependency, but keeps `metrics.py` edits sequential to avoid merge conflicts within one file). Constraining docs: da-tese-ao-produto.md section 1.2 (full metric set list).

## Design
No new pattern. File touched: `metrics.py` (extend), `tests/test_metrics.py` (extend). DRY check: grep `metrics.py` for the `y_true`/`y_pred` convention NFE-007 established and follow it exactly; grep for any existing generic "validate same length/index" helper NFE-007 wrote for its raise-on-mismatch checks and reuse it for `smape`/`mase` rather than re-implementing the same guard twice (DRY within module, per implementation-plan.md section 9).

`smape(y_true, y_pred) -> float`, `mase(y_true, y_pred, y_train, seasonal_period) -> float`.

## Implementation acceptance criteria
- [x] `smape(y_true, y_pred) -> float` and `mase(y_true, y_pred, y_train, seasonal_period) -> float` exist, typed, `mase` signature includes `y_train` for its scaling denominator (not just `y_true`/`y_pred`).

## Test acceptance criteria
- [x] Unit test: `smape` bounded in its documented range (state the convention chosen — 0-200% or 0-2 — in the docstring) and symmetric under swapping over/under-forecast direction on a synthetic example.
- [x] Unit test: `mase` returns exactly 1.0 when `y_pred` equals the naive in-sample benchmark it's scaled against, on a synthetic series.
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_metrics.py -q`, confirm pass, paste output.

## Review acceptance criteria
- Tech Lead verifies: no duplicated length/index-validation logic between `mae`/`rmse` (NFE-007) and `smape`/`mase` — a shared private helper if any validation logic repeats a second time.

## Documentation acceptance criteria
- [x] None beyond keeping `metrics.py` docstring accurate for the newly added functions' bounds/convention.
