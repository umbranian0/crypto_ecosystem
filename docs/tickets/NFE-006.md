# NFE-006 — NaiveLast baseline

**Status: done**

## Analysis
Story: NFE-006 (Must), depends on NFE-004 (parallel-safe with NFE-005 once NFE-004 lands — different concrete class, same file, but non-overlapping additions; sequenced after NFE-005 in this sprint only to keep file-edit conflicts to zero, per Tech Lead's parallelization call, not a data dependency). Constraining docs: da-tese-ao-produto.md section 2.3.1 lists Naive0/NaiveLast as a pair.

## Design
Pattern: Strategy implementation (second concrete class). File touched: `baselines.py` (add `NaiveLast`), `tests/test_baselines.py`. DRY check: grep for `Naive0`'s pattern first — `NaiveLast` should follow the exact same structural shape (implements `Baseline`, same fixture reuse), differing only in the actual forecast value logic, to avoid two divergent baseline "styles" in the same file.

`NaiveLast.predict(train, test) -> Series`: last observation in `train` carried forward, constant across `test`'s index, reading nothing past `train_end`.

## Implementation acceptance criteria
- [x] `NaiveLast` implements `Baseline`, returns the last train-period observation carried forward across test, reads no data past `train_end`.

## Test acceptance criteria
- [x] Unit test: `NaiveLast.predict` output is constant across test and equals the last value in `train`.
- [x] Unit test: on a synthetic series where the last train value is non-zero, `NaiveLast` and `Naive0` produce different outputs (guards against accidental copy-paste equivalence).
- [x] Run via `.venv\Scripts\python.exe -m pytest tests/test_baselines.py -q`, confirm pass, paste output.

## Review acceptance criteria
- Tech Lead verifies: `NaiveLast` genuinely differs from `Naive0` in the diff (not a copy with a renamed class), and the differentiation unit test actually exercises a non-zero last-train-value case.

## Documentation acceptance criteria
- [x] None beyond keeping `baselines.py` docstring accurate (should already read as accurate after NFE-005; confirm, no edit expected). Confirmed: module docstring already says "Naive0 / NaiveLast implementations."
