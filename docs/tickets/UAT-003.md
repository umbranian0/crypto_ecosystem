# UAT-003 — Headline verdict summary on run-detail page

**Module**: `services/dashboard-web`. **Depends on**: none (UAT-001 already shipped). Sequenced first in
Track B — establishes new headline element before UAT-004/UAT-013 touch the same templates.

## Analysis
Story: `docs/product/backlog-uat-findings.md` UAT-003. Must reuse RAV-003's existing `build_dm_verdict_chart`
categorization (`app/charting.py`) — no second "count better/worse splits" tally, so the headline and the
existing DM-verdict chart can never disagree. Must respect UAT-001's `has_client_model` placeholder
caveat inline in the headline text (not a second disclosure surface).

## Design
Pattern: none new — pure function + template render, DI already established.
Files touched (scoped to `services/dashboard-web`):
- `app/charting.py` — new `build_headline_verdict_summary(run, splits)` pure function, built from the
  same per-split `dm_verdict` categorization `build_dm_verdict_chart` already derives (reuse its
  category logic, e.g. factor the "better"/"worse"/"undefined" per-split classification into a shared
  helper both call, if not already a standalone function).
- `run_detail.html` — new one-line summary rendered below the status table, above the per-split
  table/charts.
**DRY check note**: grep `build_dm_verdict_chart`, `verdict_category_and_css_slug` in `app/charting.py`
before writing — reuse the existing per-split categorization exactly, do not re-derive "better than
Naive0" from raw `dm_verdict` strings a second time.

## Implementation acceptance criteria
- [x] `run_detail.html` renders a new single-line summary below the status table, above per-split
      table/charts (e.g. "Beat Naive0 on 4/7 splits, not statistically significant").
- [x] Computed via `build_headline_verdict_summary`, reusing existing per-split verdict categorization —
      no recomputation of DM statistics.
- [x] When `run.has_client_model` is `False`, headline text states the placeholder caveat inline (e.g.
      "Beat NaiveLast placeholder on 4/7 splits — no client model submitted").
- [x] Zero-split runs show no headline (same existing "no per-split results yet" branch).
- [x] All pre-existing `run_detail.html` elements unchanged.

## Test acceptance criteria
- [x] Unit test for `build_headline_verdict_summary` with a fixture where its "better" count matches
      `build_dm_verdict_chart`'s own categorization (assert they agree, not just independently correct).
- [x] Route-level test asserts the headline renders with/without `has_client_model`, and is absent for a
      zero-split run.
- [x] A test asserts all pre-existing `run_detail.html` elements still render byte-identical (per the
      story's own explicit acceptance criterion).
- [x] Full `dashboard-web` suite re-run, zero regressions (319 passed, 7 deselected, after
      UAT-004/UAT-013 landed on top).

## Review acceptance criteria (Tech Lead)
- [ ] Read diff: confirm headline reuses existing categorization function/logic, no parallel tally.
- [ ] Confirm placeholder caveat wording matches UAT-001's `model_column_label` style, not a new phrase.
- [ ] Full suite re-run personally.

## Documentation acceptance criteria
- [x] `services/dashboard-web/README.md` gets a one-line status note for UAT-003 under the existing
      run-detail feature list.

## Status: done
